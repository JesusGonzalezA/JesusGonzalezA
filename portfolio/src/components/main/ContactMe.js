import React from 'react'
import { Contact } from '../Contact'

export const ContactMe = ({ light = false }) => {
    return (
        <div id="contact">
            <Contact light/>
        </div>
    )
}
